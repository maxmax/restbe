{
  "routes": [
    {
      "method": "GET",
      "path": "/worktypes",
      "handler": "Worktypes.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/worktypes/count",
      "handler": "Worktypes.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/worktypes/:_id",
      "handler": "Worktypes.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/worktypes",
      "handler": "Worktypes.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/worktypes/:_id",
      "handler": "Worktypes.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/worktypes/:_id",
      "handler": "Worktypes.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
