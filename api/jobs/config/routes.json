{
  "routes": [
    {
      "method": "GET",
      "path": "/jobs",
      "handler": "Jobs.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/jobs/count",
      "handler": "Jobs.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/jobs/:_id",
      "handler": "Jobs.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/jobs",
      "handler": "Jobs.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/jobs/:_id",
      "handler": "Jobs.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/jobs/:_id",
      "handler": "Jobs.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}