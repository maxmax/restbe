{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-mongoose",
      "settings": {
        "client": "mongo",
        "uri": "${process.env.DATABASE_URI || ''}",
        "host": "${process.env.DATABASE_HOST || 'ds253871.mlab.com'}",
        "port": "${process.env.DATABASE_PORT || 53871}",
        "database": "${process.env.DATABASE_NAME || 'heroku_7vj4cd28'}",
        "username": "${process.env.DATABASE_USERNAME || 'heroku_7vj4cd28'}",
        "password": "${process.env.DATABASE_PASSWORD || 'db7qmuq29bqftguuoap5l8kjfh'}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || false}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}"
      }
    }
  }
}
